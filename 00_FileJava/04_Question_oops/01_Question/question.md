Q1. Abstract Class Basics

Write:

An abstract class Vehicle

It should have:

a protected int speed

an abstract method void move()

Create a class Car that:

extends Vehicle

implements move() and prints "Car is moving at <speed>"
